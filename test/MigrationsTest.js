
var Migrations = artifacts.require("Migrations.sol");

contract('Migrations', (accounts) => {
    it('deploy contract and check owner', async () => {
        // let account = accounts[0];
        // let ctx = await Migrations.deployed();
        // expect(await ctx.owner()).to.equal(account);
    });
})